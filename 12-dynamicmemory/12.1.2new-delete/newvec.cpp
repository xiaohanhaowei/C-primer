#include "newvec.h"

int main() {
    // auto p = func1();
    // cout << "pointer constructed done" << endl;
    // read(std::cin, p);
    // print(std::cout, p);
    // delete p;
    print(std::cout, read(std::cin, func1()));
    return 0;
}